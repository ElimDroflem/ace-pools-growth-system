import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const url=localStorage.getItem('aceSyncUrl'),key=localStorage.getItem('aceSyncKey'),watched=new Set(['acePoolLeads','acePoolReviews','aceSettings']);
if(url&&key){const client=createClient(url,key),original=localStorage.setItem.bind(localStorage),timer={id:null};async function push(){const {data}=await client.auth.getSession();if(!data.session)return;const payload={};watched.forEach(k=>payload[k]=JSON.parse(localStorage.getItem(k)||'null'));await client.from('ace_sales_state').upsert({user_id:data.session.user.id,payload,updated_at:new Date().toISOString()});}localStorage.setItem=(k,v)=>{original(k,v);if(watched.has(k)){clearTimeout(timer.id);timer.id=setTimeout(push,700)}};setTimeout(push,900);}
