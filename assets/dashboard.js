(()=>{const stages=['New lead','Contacted','Qualified','Review offered','Review booked','Review completed','Quote sent','Follow-up due','Won','Lost','Onboarded'];const $=id=>document.getElementById(id);const rank={High:0,Medium:1,Low:2};let leads=JSON.parse(localStorage.getItem('acePoolLeads')||'[]'),activeId=null;if(!leads.length){leads=[{id:1,name:'Example prospect',postcode:'CB23',phone:'',email:'',source:'Example',status:'New lead',priority:'High',nextAction:'Replace this example with a live map lead',nextDate:new Date().toISOString().slice(0,10),notes:'Open the Lead Map and add a prospect.',whyNow:'Example only',pain:'Example only',offer:'Complimentary Pool Review',angle:'Example only',history:''}];save()}function save(){localStorage.setItem('acePoolLeads',JSON.stringify(leads))}function escape(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function render(){const q=$('search').value.toLowerCase(),sf=$('statusFilter').value,pf=$('priorityFilter').value;const rows=leads.filter(l=>(!q||`${l.name} ${l.postcode}`.toLowerCase().includes(q))&&(!sf||l.status===sf)&&(!pf||l.priority===pf)).sort((a,b)=>(rank[a.priority]-rank[b.priority])||String(a.nextDate).localeCompare(String(b.nextDate)));$('leadRows').innerHTML=rows.map(l=>`<tr><td class="priority ${l.priority.toLowerCase()}">${escape(l.priority)}</td><td><strong>${escape(l.name)}</strong><br><small>${escape(l.postcode||'')}</small></td><td>${escape(l.whyNow||l.notes||l.source)}</td><td>${escape(l.status)}</td><td>${escape(l.nextAction||'')}</td><td>${escape(l.nextDate||'')}</td><td><button class="btn btn-dark" data-work="${l.id}">Work deal</button></td></tr>`).join('')||'<tr><td colspan="7">No matching leads.</td></tr>';$('kpiNew').textContent=leads.filter(l=>l.status==='New lead').length;$('kpiReviews').textContent=leads.filter(l=>l.status==='Review booked').length;$('kpiQuotes').textContent=leads.filter(l=>l.status==='Quote sent').length;$('kpiWon').textContent=leads.filter(l=>['Won','Onboarded'].includes(l.status)).length;document.querySelectorAll('[data-work]').forEach(x=>x.onclick=()=>openDeal(x.dataset.work))}function openDeal(id){const l=leads.find(x=>String(x.id)===String(id));if(!l)return;activeId=l.id;$('dealTitle').textContent=l.name;$('dealMeta').textContent=[l.postcode,l.source,l.score?`Score ${l.score}`:''].filter(Boolean).join(' · ');$('dealWhy').textContent=l.whyNow||'Verify the trigger before contact.';$('dealPain').textContent=l.pain||l.notes||'Qualify the pool condition and current service arrangement.';$('dealOffer').textContent=l.offer||'Complimentary Pool Review';$('dealAngle').textContent=l.angle||'Offer a clear review of safety, condition and avoidable running costs.';$('dealEvidence').href=l.url||'#';$('dealEvidence').style.display=l.url?'inline-flex':'none';$('dealStatus').value=l.status||'New lead';$('dealPriority').value=l.priority||'Medium';$('dealPhone').value=l.phone||'';$('dealEmail').value=l.email||'';$('dealNextAction').value=l.nextAction||'';$('dealNextDate').value=l.nextDate||'';$('dealOutcome').value='';$('dealInteraction').value='';$('dealHistory').value=l.history||'';$('dealDialog').showModal()}stages.forEach(s=>{$('statusFilter').insertAdjacentHTML('beforeend',`<option>${s}</option>`);$('dealStatus').insertAdjacentHTML('beforeend',`<option>${s}</option>`)});['search','statusFilter','priorityFilter'].forEach(id=>$(id).oninput=render);$('addLead').onclick=()=>$('leadDialog').showModal();$('leadForm').onsubmit=e=>{if(e.submitter?.value==='cancel')return;leads.unshift({id:Date.now(),created:new Date().toISOString(),name:$('leadName').value.trim(),postcode:$('leadPostcode').value.trim(),phone:$('leadPhone').value.trim(),email:$('leadEmail').value.trim(),source:$('leadSource').value,status:'New lead',priority:$('leadPriority').value,nextAction:'Make first contact',nextDate:new Date(Date.now()+86400000).toISOString().slice(0,10),notes:$('leadNotes').value.trim(),history:''});save();setTimeout(render,0);$('leadForm').reset()};$('dealForm').onsubmit=e=>{if(e.submitter?.value==='cancel')return;const l=leads.find(x=>String(x.id)===String(activeId));if(!l)return;l.status=$('dealStatus').value;l.priority=$('dealPriority').value;l.phone=$('dealPhone').value.trim();l.email=$('dealEmail').value.trim();l.nextAction=$('dealNextAction').value.trim();l.nextDate=$('dealNextDate').value;const note=$('dealInteraction').value.trim(),outcome=$('dealOutcome').value;if(note||outcome){const stamp=new Date().toLocaleString('en-GB');l.history=`[${stamp}] ${outcome}${outcome&&note?' – ':''}${note}\n${l.history||''}`}save();setTimeout(render,0)};$('copyOpening').onclick=async()=>{await navigator.clipboard.writeText($('dealAngle').textContent);$('copyOpening').textContent='Copied';setTimeout(()=>$('copyOpening').textContent='Copy opening',1200)};$('removeDeal').onclick=()=>{if(confirm('Remove this deal from the pipeline?')){leads=leads.filter(x=>String(x.id)!==String(activeId));save();$('dealDialog').close();render()}};$('exportCsv').onclick=()=>{const keys=['name','postcode','phone','email','source','status','priority','nextAction','nextDate','whyNow','pain','offer','angle','url','history'];const csv=[keys.join(','),...leads.map(l=>keys.map(k=>`"${String(l[k]||'').replace(/"/g,'""')}"`).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='ace-pools-deals.csv';a.click();URL.revokeObjectURL(a.href)};$('importCsv').onchange=e=>{const f=e.target.files[0];if(!f)return;const reader=new FileReader();reader.onload=()=>{const lines=reader.result.split(/\r?\n/).filter(Boolean),headers=parse(lines.shift());for(const line of lines){const vals=parse(line),obj={id:Date.now()+Math.random(),status:'New lead',priority:'Medium',nextAction:'Review imported record',nextDate:new Date(Date.now()+86400000).toISOString().slice(0,10)};headers.forEach((h,i)=>obj[h.trim()]=vals[i]||'');leads.push(obj)}save();render()};reader.readAsText(f)};function parse(line){const out=[];let v='',q=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"'&&line[i+1]==='"'){v+='"';i++}else if(c==='"')q=!q;else if(c===','&&!q){out.push(v);v=''}else v+=c}out.push(v);return out}render();const requested=new URLSearchParams(location.search).get('lead');if(requested)setTimeout(()=>openDeal(requested),0)})();
